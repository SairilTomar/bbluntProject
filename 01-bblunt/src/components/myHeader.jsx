import { BsBrightnessLow } from "react-icons/bs"

{/* <div className="w-full">
  <div className="flex items-center lg:justify-between px-4 py-4">

    {/* LEFT SIDE */}
    <div className="flex items-center gap-3">

      {/* Menu Icon - Mobile Only */}
      <div className="
      flex items-center gap-2 ">
        <CgMenuLeft className='lg:hidden' size={24} />
      {/* Logo */}
      <img
        className="w-[80px] lg:w-[115px] object-contain"
        src={logo}
        alt="Logo"
      />
      </div>
      {/* Desktop Menu */}
      <div className="hidden lg:flex gap-5 items-center">
        {leftmenu.map((item, index) => (
          <HeaderItemLeft
            key={index}
            Icon={item.icon}
            name={item.name}
          />
        ))}
      </div>
    </div>

    {/* RIGHT SIDE */}
      {/* Get App - Mobile */}
      <div className="lg:hidden pl-40 flex items-center gap-8">
  <input
    type="button"
    value="Get App"
    className="bg-black text-white text-[14px] px-3 py-1 rounded-full cursor-pointer"
  />

</div>

      {/* Mobile Search Icon */}
      <div className="lg:hidden block pr-1 pl-4">
        <CiSearch className="text-[20px]" strokeWidth={1.5} />
      </div>

      {/* Desktop Search */}
      <div className='gap-5 flex pr-1'>
        <div className="hidden lg:block relative w-64">
        <CiSearch
          className="absolute font-bold left-3 top-1/2 -translate-y-1/2"
          strokeWidth={1.5}
        />
        <SearchInput />
      </div>
      {/* Cart */}
    
       <div className="flex items-center gap-1">
        <BsCart2 className="text-[20px] font-bold"/>
        <span className="hidden lg:inline">Cart</span>
      </div>

      {/* Account - Desktop Only */}
      <div className="hidden lg:flex items-center gap-1">
        <FaRegUser />
        <span>Account</span>
      </div>
 
      </div>

    </div>
  </div>




/////////
<div className="w-full">
  <div className="flex items-center justify-between px-4 py-4">

    {/* LEFT SIDE */}
    <div className="flex items-center gap-6">

      {/* Mobile Menu Icon */}
      <CgMenuLeft className="lg:hidden" size={24} />

      {/* Logo */}
      <img
        className="w-[80px] lg:w-[115px] object-contain"
        src={logo}
        alt="Logo"
      />

      {/* Desktop Menu */}
      <div className="hidden lg:flex gap-6 items-center">
        {leftmenu.map((item, index) => (
          <HeaderItemLeft
            key={index}
            Icon={item.icon}
            name={item.name}
          />
        ))}
      </div>
    </div>

    {/* RIGHT SIDE */}
    <div className="flex items-center gap-4">

      {/* Mobile Get App */}
      <input
        type="button"
        value="Get App"
        className="lg:hidden bg-black text-white text-[13px] px-3 py-1 rounded-full cursor-pointer"
      />

      {/* Mobile Search */}
      <CiSearch
        className="lg:hidden text-[20px]"
        strokeWidth={1.5}
      />

      {/* Desktop Search */}
      <div className="hidden lg:block relative w-64 gap-8">
        <CiSearch
          className="absolute left-3 top-1/2 -translate-y-1/2"
          strokeWidth={1.5}
        />
        <SearchInput />
      </div>

      {/* Cart */}
      <div className="flex items-center gap-1">
        <BsCart2 className="text-[20px]" />
        <span className="hidden lg:inline">Cart</span>
      </div>

      {/* Account - Desktop */}
      <div className="hidden lg:flex items-center gap-1">
        <FaRegUser />
        <span>Account</span>
      </div>

    </div>

  </div>
</div>
 */}



//  final one Below 
//   <div className="w-full">
//   <div className="flex items-center justify-between px-4 py-4">

//     {/* LEFT SIDE */}
//     <div className="flex items-center gap-8">

//       {/* Logo */}
//       <img
//         className="w-[115px] object-contain"
//         src={logo}
//         alt="Logo"
//       />

//       {/* Desktop Menu */}
//       <div className="hidden lg:flex gap-6 items-center">
//         {leftmenu.map((item, index) => (
//           <HeaderItemLeft
//             key={index}
//             Icon={item.icon}
//             name={item.name}
//           />
//         ))}
//       </div>
//     </div>

//     {/* RIGHT SIDE */}
//     <div className="flex items-center gap-6">

//       {/* Desktop Search */}
//       <div className="hidden lg:block relative w-64">
//         <CiSearch
//           className="absolute left-3 top-1/2 -translate-y-1/2"
//           strokeWidth={1.5}
//         />
//         <SearchInput />
//       </div>

//       {/* Cart */}
//       <div className="flex items-center gap-1">
//         <BsCart2 />
//         <span className=" text-[14px] font-extrabold hidden lg:inline">Cart</span>
//       </div>

//       {/* Account */}
//       <div className="hidden lg:flex items-center gap-1">
//         <FaRegUser />
//         <span className='text-[14px] font-bold'>Account</span>
//       </div>

//     </div>

//   </div>
// </div>
